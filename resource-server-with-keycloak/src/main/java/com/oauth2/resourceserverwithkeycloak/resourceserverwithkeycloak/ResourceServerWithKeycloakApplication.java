package com.oauth2.resourceserverwithkeycloak.resourceserverwithkeycloak;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class ResourceServerWithKeycloakApplication {

	public static void main(String[] args) {
		SpringApplication.run(ResourceServerWithKeycloakApplication.class, args);
	}

}
